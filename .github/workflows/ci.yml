name: CI

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  test:
    name: Check and test
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Set up just
        uses: taiki-e/install-action@v2
        with:
          tool: just

      - name: Set up Electron UI test dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y imagemagick xvfb

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Check
        run: bun run check

      - name: Test
        run: bun test

      - name: UI test
        run: xvfb-run -a bun run test:ui
